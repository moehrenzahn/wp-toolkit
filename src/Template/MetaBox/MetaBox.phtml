<?php
/** @var \Moehrenzahn\Toolkit\View\Post\MetaBox $view */
foreach ($view->getPostPreferences() as $preference) {
    $view->renderPartial(
        TOOLKIT_TEMPLATE_FOLDER . "MetaBox/Setting/$preference->type",
            null,
        ['preference' => $preference]
    );
}